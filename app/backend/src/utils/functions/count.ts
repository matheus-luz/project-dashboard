// import { TCity } from '../../types/TCity';
// import { TClient } from '../../types/TClient';

// export default class CountFunctions {
//   public countClients = (clients: TClient[], cities: TCity[]) => {
//     let count = 0;
//     clients.foreach((client: any) => {
//       cities.forEach((city) => {
//         if (client.city_id === city.id) {
//           count += 1;
//         }
//       });
//     });
//     return count;
//   };
// }